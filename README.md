# HideChat

HideChat is a Web privacy chat project scaffolded from the repository documents.

Current phase:

- standardized repository layout
- Spring Boot backend skeleton
- frontend directory skeleton
- initial database migration draft

The original source documents remain under `HideChatDocs/`. Root-level `agents.md` and `docs/` were mirrored to match the documented repository layout.
