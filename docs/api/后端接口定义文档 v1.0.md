# 《后端接口定义文档 v1.0》

适用范围：

* 后端：Spring Boot 3.x
* 认证：邮箱登录 + JWT + Refresh Token
* 通信：REST API + WebSocket
* 当前范围：MVP / V1
* 场景：Web 端 1V1 聊天、联系人、会话列表、图片上传、忘记密码

---

# 1. 文档目标

本文档定义系统后端 REST 接口：

* 接口分组
* 请求/响应规范
* 鉴权方式
* 字段定义
* 错误码约定
* 幂等与安全要求
* 与前端本地加密边界

说明：

* **幸运数字校验默认在前端本地完成**
* **PIN 解锁本地聊天记录默认在前端完成**
* 后端负责：

  * 账号体系
  * 联系人 / 会话 / 消息
  * 文件上传
  * 未读管理
  * 补偿拉取

---

# 2. 接口设计原则

## 2.1 风格

采用 RESTful 风格，统一前缀：

```text
/api
```

## 2.2 传输格式

* 请求：`application/json`
* 响应：`application/json`

## 2.3 时间格式

* 对外推荐返回：

  * 毫秒时间戳 `long`
* 后端内部：

  * `LocalDateTime`

## 2.4 认证方式

除注册、登录、发验证码、重置密码外，其他接口默认需要：

```http
Authorization: Bearer <accessToken>
```

---

# 3. 统一响应结构

所有 REST 接口统一返回：

```json
{
  "code": 0,
  "message": "success",
  "data": {}
}
```

---

## 3.1 响应字段说明

| 字段      | 类型                    | 说明           |
| ------- | --------------------- | ------------ |
| code    | int                   | 业务状态码，0 表示成功 |
| message | string                | 响应描述         |
| data    | object / array / null | 业务数据         |

---

## 3.2 成功示例

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "userUid": "u_1001"
  }
}
```

---

## 3.3 失败示例

```json
{
  "code": 400100,
  "message": "邮箱验证码无效",
  "data": null
}
```

---

# 4. 通用错误码定义

| 错误码    | 含义            |
| ------ | ------------- |
| 0      | 成功            |
| 400001 | 参数错误          |
| 400002 | 缺少必填参数        |
| 400003 | 参数格式非法        |
| 401001 | 未登录或 token 无效 |
| 401002 | token 已过期     |
| 403001 | 无权限访问         |
| 404001 | 资源不存在         |
| 409001 | 资源已存在         |
| 429001 | 请求过于频繁        |
| 500001 | 系统内部错误        |
| 500002 | 数据库异常         |
| 500003 | 文件服务异常        |

认证相关：

| 错误码    | 含义               |
| ------ | ---------------- |
| 410101 | 邮箱已注册            |
| 410102 | 用户不存在            |
| 410103 | 密码错误             |
| 410104 | 邮箱验证码无效          |
| 410105 | 邮箱验证码已过期         |
| 410106 | 邮箱验证码已使用         |
| 410107 | 发送验证码过于频繁        |
| 410108 | refresh token 无效 |

聊天相关：

| 错误码    | 含义       |
| ------ | -------- |
| 420101 | 联系人不存在   |
| 420102 | 会话不存在    |
| 420103 | 消息不存在    |
| 420104 | 文件不存在    |
| 420105 | 不支持的消息类型 |

---

# 5. 鉴权说明

## 5.1 Access Token

* 用于普通 API 访问
* 放在请求头：

```http
Authorization: Bearer <accessToken>
```

## 5.2 Refresh Token

* 用于换取新的 access token
* 只在刷新接口中使用

---

# 6. 接口分组总览

本文档包含以下接口分组：

1. Auth 认证模块
2. User 用户模块
3. Contact 联系人模块
4. Conversation 会话模块
5. Message 消息模块
6. File 文件模块
7. System / Disguise 伪装与系统模块

---

# 7. Auth 模块接口

---

# 7.1 发送邮箱验证码

## 接口

`POST /api/auth/email/send-code`

## 说明

用于：

* 注册
* 邮箱验证码登录
* 找回密码

## 鉴权

不需要

## 请求体

```json
{
  "email": "alice@example.com",
  "bizType": "register"
}
```

## 请求字段

| 字段      | 类型     | 必填 | 说明                                |
| ------- | ------ | -: | --------------------------------- |
| email   | string |  是 | 邮箱地址                              |
| bizType | string |  是 | register / login / reset_password |

## 响应

```json
{
  "code": 0,
  "message": "success",
  "data": null
}
```

## 错误场景

* 邮箱格式非法
* 发送过于频繁
* 邮件服务异常

---

# 7.2 邮箱注册

## 接口

`POST /api/auth/email/register`

## 鉴权

不需要

## 请求体

```json
{
  "email": "alice@example.com",
  "password": "Abcd1234",
  "emailCode": "123456",
  "nickname": "Alice"
}
```

## 请求字段

| 字段        | 类型     | 必填 | 说明    |
| --------- | ------ | -: | ----- |
| email     | string |  是 | 邮箱    |
| password  | string |  是 | 登录密码  |
| emailCode | string |  是 | 邮箱验证码 |
| nickname  | string |  是 | 昵称    |

## 响应

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "userUid": "u_1001"
  }
}
```

## 业务规则

* 邮箱不可重复注册
* 验证码必须可用且未过期
* 密码强度校验

---

# 7.3 邮箱密码登录

## 接口

`POST /api/auth/email/password-login`

## 鉴权

不需要

## 请求体

```json
{
  "email": "alice@example.com",
  "password": "Abcd1234"
}
```

## 响应

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "accessToken": "xxx",
    "refreshToken": "xxx",
    "expiresIn": 7200,
    "userInfo": {
      "userUid": "u_1001",
      "nickname": "Alice",
      "avatarUrl": ""
    }
  }
}
```

---

# 7.4 邮箱验证码登录

## 接口

`POST /api/auth/email/code-login`

## 鉴权

不需要

## 请求体

```json
{
  "email": "alice@example.com",
  "emailCode": "123456"
}
```

## 响应

同密码登录。

---

# 7.5 重置密码

## 接口

`POST /api/auth/email/reset-password`

## 鉴权

不需要

## 请求体

```json
{
  "email": "alice@example.com",
  "emailCode": "123456",
  "newPassword": "NewPass1234"
}
```

## 响应

```json
{
  "code": 0,
  "message": "success",
  "data": null
}
```

## 业务规则

* 验证成功后，旧 refresh token 全部失效
* 要求用户重新登录

---

# 7.6 刷新 Token

## 接口

`POST /api/auth/refresh-token`

## 鉴权

不需要

## 请求体

```json
{
  "refreshToken": "xxx"
}
```

## 响应

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "accessToken": "new_access_token",
    "refreshToken": "new_refresh_token",
    "expiresIn": 7200
  }
}
```

---

# 7.7 登出

## 接口

`POST /api/auth/logout`

## 鉴权

需要

## 请求体

```json
{
  "refreshToken": "xxx"
}
```

## 响应

```json
{
  "code": 0,
  "message": "success",
  "data": null
}
```

## 业务规则

* 服务端使 refresh token 失效
* 前端应清理 access token

---

# 8. User 模块接口

---

# 8.1 获取当前用户信息

## 接口

`GET /api/user/me`

## 鉴权

需要

## 响应

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "userUid": "u_1001",
    "nickname": "Alice",
    "avatarUrl": "",
    "email": "alice@example.com"
  }
}
```

---

# 8.2 更新用户资料

## 接口

`PUT /api/user/profile`

## 鉴权

需要

## 请求体

```json
{
  "nickname": "Alice New",
  "avatarUrl": "https://cdn.example.com/avatar.jpg"
}
```

## 响应

```json
{
  "code": 0,
  "message": "success",
  "data": null
}
```

---

# 8.3 搜索用户

## 接口

`GET /api/user/search?keyword=alice`

## 鉴权

需要

## 响应

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "userUid": "u_1002",
      "nickname": "Bob",
      "avatarUrl": ""
    }
  ]
}
```

## 业务规则

* MVP 不建议支持过强模糊搜索
* 可按昵称前缀 / 邮箱精确匹配
* 需要限流

---

# 9. Contact 模块接口

---

# 9.1 添加联系人

## 接口

`POST /api/contact/add`

## 鉴权

需要

## 请求体

```json
{
  "peerUid": "u_1002",
  "remarkName": "Bob"
}
```

## 响应

```json
{
  "code": 0,
  "message": "success",
  "data": null
}
```

## 业务规则

* 不能添加自己
* 重复添加应幂等处理

---

# 9.2 获取联系人列表

## 接口

`GET /api/contact/list`

## 鉴权

需要

## 响应

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "peerUid": "u_1002",
      "peerNickname": "Bob",
      "peerAvatar": "",
      "remarkName": "Bob",
      "pinned": false,
      "lastMessageAt": 1712300000000
    }
  ]
}
```

---

# 9.3 更新联系人备注/置顶状态

## 接口

`PUT /api/contact/update`

## 鉴权

需要

## 请求体

```json
{
  "peerUid": "u_1002",
  "remarkName": "新备注",
  "pinned": true
}
```

## 响应

```json
{
  "code": 0,
  "message": "success",
  "data": null
}
```

---

# 9.4 删除联系人

## 接口

`DELETE /api/contact/{peerUid}`

## 鉴权

需要

## 响应

```json
{
  "code": 0,
  "message": "success",
  "data": null
}
```

## 说明

* 删除联系人不一定删除会话和消息
* 推荐只删除联系人展示关系

---

# 10. Conversation 模块接口

---

# 10.1 获取会话列表

## 接口

`GET /api/conversation/list`

## 鉴权

需要

## 说明

返回用户已存在的 1V1 会话列表。

## 响应

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "conversationId": "c_1001",
      "peerUid": "u_1002",
      "peerNickname": "Bob",
      "peerAvatar": "",
      "remarkName": "Bob",
      "lastMessageType": "text",
      "lastMessagePreview": "[文本消息]",
      "lastMessageAt": 1712300000000,
      "unreadCount": 2,
      "pinned": false
    }
  ]
}
```

## 注意

* 不返回本地 PIN 材料
* 不返回本地密文缓存
* 只返回云端索引数据

---

# 10.2 获取或创建 1V1 会话

## 接口

`POST /api/conversation/single`

## 鉴权

需要

## 请求体

```json
{
  "peerUid": "u_1002"
}
```

## 响应

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "conversationId": "c_1001",
    "peerUid": "u_1002"
  }
}
```

## 业务规则

* 如果会话存在，直接返回
* 不存在则创建
* 需保证 user pair 去重
* 调用前需已添加对方为联系人，否则返回 420101

---

# 10.3 获取会话详情

## 接口

`GET /api/conversation/{conversationId}`

## 鉴权

需要

## 响应

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "conversationId": "c_1001",
    "peerUid": "u_1002",
    "peerNickname": "Bob",
    "peerAvatar": "",
    "remarkName": "Bob",
    "createdAt": 1712200000000
  }
}
```

---

# 10.4 清空未读数

## 接口

`POST /api/conversation/clear-unread`

## 鉴权

需要

## 请求体

```json
{
  "conversationId": "c_1001"
}
```

## 响应

```json
{
  "code": 0,
  "message": "success",
  "data": null
}
```

---

# 11. Message 模块接口

---

# 11.1 拉取历史消息

## 接口

`GET /api/message/history`

## 鉴权

需要

## 请求参数

| 参数             | 类型     | 必填 | 说明           |
| -------------- | ------ | -: | ------------ |
| conversationId | string |  是 | 会话 ID        |
| cursor         | string |  否 | 游标，首次为空      |
| pageSize       | int    |  否 | 默认 20，最大 100 |

示例：

```text
/api/message/history?conversationId=c_1001&cursor=1712300000000&pageSize=20
```

## 响应

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [
      {
        "messageId": "m_1001",
        "conversationId": "c_1001",
        "senderUid": "u_1002",
        "receiverUid": "u_1001",
        "messageType": "text",
        "payloadType": "encrypted",
        "payload": "xxxxx",
        "fileId": null,
        "clientMsgTime": 1712300000000,
        "serverMsgTime": 1712300001000,
        "serverStatus": "delivered"
      }
    ],
    "nextCursor": "1712290000000",
    "hasMore": true
  }
}
```

## 说明

* 前端优先读本地 IndexedDB
* 此接口主要用于：

  * 首次同步
  * 补偿拉取
  * 跨设备扩展准备

---

# 11.2 标记消息已读

## 接口

`POST /api/message/read`

## 鉴权

需要

## 请求体

```json
{
  "conversationId": "c_1001",
  "messageIds": ["m_1001", "m_1002"]
}
```

## 响应

```json
{
  "code": 0,
  "message": "success",
  "data": null
}
```

## 业务规则

* 更新已读表
* 更新未读数
* 可通过 WebSocket 通知对端

---

# 11.3 删除单条消息（逻辑删除）

## 接口

`DELETE /api/message/{messageId}`

## 鉴权

需要

## 响应

```json
{
  "code": 0,
  "message": "success",
  "data": null
}
```

## 说明

* 默认只做逻辑删除
* 不承诺删除对端本地缓存

---

# 11.4 批量获取未送达/补偿消息

## 接口

`GET /api/message/pending?since=1712300000000`

## 鉴权

需要

## 响应

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "messageId": "m_1003",
      "conversationId": "c_1001",
      "senderUid": "u_1002",
      "receiverUid": "u_1001",
      "messageType": "text",
      "payloadType": "encrypted",
      "payload": "xxxxx",
      "serverMsgTime": 1712300010000
    }
  ]
}
```

## 用途

* WebSocket 重连后补偿
* 多标签页状态恢复

---

# 12. File 模块接口

---

# 12.1 获取上传签名

## 接口

`POST /api/file/upload-sign`

## 鉴权

需要

## 请求体

```json
{
  "fileName": "image.jpg",
  "mimeType": "image/jpeg",
  "fileSize": 123456,
  "encryptFlag": true
}
```

## 响应

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "fileId": "f_1001",
    "uploadUrl": "https://storage.example.com/xxx",
    "storageKey": "chat/2026/04/06/abc.bin",
    "headers": {
      "Content-Type": "application/octet-stream"
    }
  }
}
```

## 说明

推荐前端直传对象存储。

---

# 12.2 上传完成回调

## 接口

`POST /api/file/complete`

## 鉴权

需要

## 请求体

```json
{
  "fileId": "f_1001",
  "storageKey": "chat/2026/04/06/abc.bin",
  "mimeType": "image/jpeg",
  "fileSize": 123456,
  "encryptFlag": true
}
```

## 响应

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "fileId": "f_1001",
    "accessUrl": "https://cdn.example.com/chat/2026/04/06/abc.bin"
  }
}
```

---

# 12.3 获取文件信息

## 接口

`GET /api/file/{fileId}`

## 鉴权

需要

## 响应

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "fileId": "f_1001",
    "mimeType": "image/jpeg",
    "fileSize": 123456,
    "accessUrl": "https://cdn.example.com/chat/2026/04/06/abc.bin",
    "encryptFlag": true
  }
}
```

---

# 13. System / Disguise 模块接口

---

# 13.1 获取今日运势内容

## 接口

`GET /api/system/fortune/today`

## 鉴权

不需要

## 说明

此接口仅用于伪装页内容展示，不承担幸运数字校验。

## 响应

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "title": "今日运势",
    "summary": "今天适合整理情绪与节奏。",
    "luckyColor": "蓝色",
    "luckyDirection": "东南",
    "advice": "在重要对话中保持耐心。"
  }
}
```

---

# 13.2 获取伪装页配置

## 接口

`GET /api/system/disguise-config`

## 鉴权

不需要

## 响应

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "siteTitle": "今日运势",
    "showFortuneInput": true,
    "theme": "default"
  }
}
```

---

# 14. WebSocket 协议补充说明

虽然你要求的是后端接口文档，但消息实时发送核心走 WebSocket，这里做最小补充。

---

## 14.1 连接地址

```text
wss://domain/ws/chat
```

握手时携带：

* `Authorization: Bearer <accessToken>`
  或
* query 参数 token

---

## 14.2 客户端发送消息

```json
{
  "type": "CHAT_SEND",
  "data": {
    "messageId": "m_1001",
    "conversationId": "c_1001",
    "receiverUid": "u_1002",
    "messageType": "text",
    "payloadType": "encrypted",
    "payload": "xxxx",
    "fileId": null,
    "clientMsgTime": 1712300000000
  }
}
```

---

## 14.3 服务端推送消息

```json
{
  "type": "CHAT_RECEIVE",
  "data": {
    "messageId": "m_1001",
    "conversationId": "c_1001",
    "senderUid": "u_1002",
    "receiverUid": "u_1001",
    "messageType": "text",
    "payloadType": "encrypted",
    "payload": "xxxx",
    "fileId": null,
    "serverMsgTime": 1712300001000,
    "serverStatus": "delivered"
  }
}
```

---

## 14.4 ACK 回执

```json
{
  "type": "CHAT_ACK",
  "data": {
    "messageId": "m_1001",
    "status": "server_received"
  }
}
```

---

## 14.5 已读通知

```json
{
  "type": "CHAT_READ",
  "data": {
    "conversationId": "c_1001",
    "messageIds": ["m_1001"],
    "readerUid": "u_1001",
    "readAt": 1712300005000
  }
}
```

---

# 15. DTO 建议清单

建议至少定义以下 DTO。

## Auth

* `SendEmailCodeRequest`
* `EmailRegisterRequest`
* `EmailPasswordLoginRequest`
* `EmailCodeLoginRequest`
* `ResetPasswordRequest`
* `RefreshTokenRequest`
* `LogoutRequest`

## User

* `UpdateProfileRequest`

## Contact

* `AddContactRequest`
* `UpdateContactRequest`

## Conversation

* `CreateSingleConversationRequest`
* `ClearUnreadRequest`

## Message

* `ReadMessageRequest`

## File

* `FileUploadSignRequest`
* `FileCompleteRequest`

---

# 16. VO 建议清单

## Auth

* `LoginResponseVO`
* `UserLoginInfoVO`

## User

* `UserProfileVO`
* `UserSearchItemVO`

## Contact

* `ContactItemVO`

## Conversation

* `ConversationListItemVO`
* `ConversationDetailVO`

## Message

* `MessageItemVO`
* `MessageHistoryVO`

## File

* `FileUploadSignVO`
* `FileInfoVO`

## System

* `FortuneTodayVO`
* `DisguiseConfigVO`

---

# 17. 安全要求

## 17.1 认证接口

* 限流
* 邮箱验证码接口防刷
* 密码错误次数限制可后续增强

## 17.2 用户搜索接口

* 必须鉴权
* 限流
* 不应返回过多隐私字段

## 17.3 文件上传接口

* 限制 MIME 类型
* 限制文件大小
* 建议只允许图片

## 17.4 消息接口

* 校验会话归属
* 校验消息归属
* 不允许跨会话读取消息

---

# 18. 幂等要求

## 18.1 推荐幂等接口

以下接口建议支持幂等：

* 添加联系人
* 创建 1V1 会话
* 文件 complete 回调
* WebSocket 消息发送（以 `messageId` 幂等）

## 18.2 幂等方式

* 业务主键去重
* 唯一约束拦截
* 应用层查重后返回已有结果

---

# 19. 分页与游标建议

## 19.1 会话列表

MVP 可先不分页，后续补。

## 19.2 消息列表

建议用游标，不建议 offset。

推荐：

* `cursor = 上一页最早消息时间戳 or messageId`

---

# 20. 前后端职责边界再次确认

## 前端负责

* 幸运数字校验
* PIN 校验
* 会话密钥解锁
* IndexedDB 密文存储

## 后端负责

* 登录与认证
* 联系人
* 会话索引
* 消息路由
* 消息补偿
* 文件元数据

---

# 21. 最小可开发接口集

如果要先启动 MVP 开发，建议优先做这些接口：

## Auth

* `POST /api/auth/email/send-code`
* `POST /api/auth/email/register`
* `POST /api/auth/email/password-login`
* `POST /api/auth/email/reset-password`
* `POST /api/auth/refresh-token`
* `POST /api/auth/logout`

## User

* `GET /api/user/me`
* `GET /api/user/search`

## Contact

* `POST /api/contact/add`
* `GET /api/contact/list`

## Conversation

* `GET /api/conversation/list`
* `POST /api/conversation/single`
* `POST /api/conversation/clear-unread`

## Message

* `GET /api/message/history`
* `POST /api/message/read`
* `GET /api/message/pending`

## File

* `POST /api/file/upload-sign`
* `POST /api/file/complete`
* `GET /api/file/{fileId}`

## System

* `GET /api/system/fortune/today`

